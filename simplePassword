# Simple password :)))
#python

userPw = input('Please enter your Password: ')
print("\tLet's see if my project is working\n")
trial = 0
while True:
   password = input('Password: ')
   if trial < 3:

       if password == userPw:
           print("\tpassword is CORRECT")
           print('\tYou may now continue...\n')
           break
       else:
           print('password is INCORRECT')
           trial = trial + 1
   if trial == 3:
       print("Please try TOMORROW")
       break
print(trial, 'wrong attempt/s')
